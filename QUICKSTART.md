# ⚡ Быстрый старт

## 📋 Что нужно получить перед запуском

### 1. Telegram Bot Token
1. Откройте [@BotFather](https://t.me/BotFather)
2. `/newbot` → следуйте инструкциям
3. Скопируйте токен (выглядит как: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2. Настройка Telegram Stars (платежи)
1. [@BotFather](https://t.me/BotFather) → `/mybots`
2. Выберите бота → **Payments** → **Telegram Stars**
3. Активируйте

### 3. OpenAI API Key
1. [platform.openai.com](https://platform.openai.com) → Sign up
2. [API Keys](https://platform.openai.com/api-keys) → Create new key
3. Пополните баланс ($5+)

## 🚀 Запуск за 3 шага

### Шаг 1: Настройка окружения
```bash
cd ~/Desktop/tarot-telegram-bot
cp .env.example .env
nano .env  # или любой другой редактор
```

Вставьте ваши ключи:
```env
BOT_TOKEN=ваш_токен_от_BotFather
OPENAI_API_KEY=ваш_ключ_OpenAI
```

Сохраните (Ctrl+X → Y → Enter в nano)

### Шаг 2: Запуск
```bash
npm start
```

Должно появиться:
```
✨ Tarot Telegram Bot started successfully!
🔮 Tarot Bot is running...
```

### Шаг 3: Тестирование
1. Найдите бота в Telegram
2. Отправьте `/start`
3. Выберите расклад
4. Готово! 🎉

## 🛠️ Команды

```bash
# Запуск бота
npm start

# Остановка бота
Ctrl + C

# Проверка логов
# (логи выводятся в консоль)
```

## ❓ Проблемы?

### Бот не запускается
```bash
# Проверьте .env файл
cat .env

# Переустановите зависимости
rm -rf node_modules package-lock.json
npm install
```

### OpenAI ошибка
- Проверьте баланс: [platform.openai.com/usage](https://platform.openai.com/usage)
- Проверьте правильность ключа

### Платежи не работают
- Telegram Stars активированы в @BotFather?
- У пользователя есть Stars?
- Telegram Stars доступны не во всех регионах

## 📊 Структура

```
tarot-telegram-bot/
├── index.js                 # Запуск бота
├── src/
│   ├── bot.js              # Логика бота
│   ├── data/
│   │   ├── tarotCards.js   # 78 карт Таро
│   │   └── spreadConfig.js # Настройки раскладов
│   └── services/
│       ├── openaiService.js  # ChatGPT интеграция
│       └── tarotService.js   # Логика раскладов
└── .env                    # Ваши ключи (не коммитить!)
```

## 💰 Изменение цен

Откройте `src/data/spreadConfig.js`:

```javascript
oneCard: {
  price: 50,  // ⬅️ Измените здесь
  // ...
}
```

## 🎨 Изменение стиля

Откройте `src/services/openaiService.js` → метод `createTarotPrompt()`

---

Готово! Удачи с ботом 🔮✨
